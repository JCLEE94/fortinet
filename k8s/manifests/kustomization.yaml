apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fortinet

resources:
  - namespace.yaml
  - persistent-volumes.yaml
  - pvc.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - test-deployment.yaml

images:
  - name: registry.jclee.me/fortinet
    newTag: 6e29ef5

labels:
  - includeSelectors: true
    pairs:
      app: fortinet
      managed-by: argocd

commonAnnotations:
  app.kubernetes.io/version: "6e29ef5"
  deployed-by: "github-actions-direct"
  deployed-at: "2025-07-04T16:45:00Z"