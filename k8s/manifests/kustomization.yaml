apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fortinet

resources:
  - namespace.yaml
  - persistent-volumes.yaml
  - pvc.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - test-deployment.yaml

images:
  - name: registry.jclee.me/fortinet
    newTag: 27e045b

labels:
  - includeSelectors: true
    pairs:
      app: fortinet
      managed-by: argocd

commonAnnotations:
  app.kubernetes.io/version: "27e045b"
  deployed-by: "github-actions-restored"
  deployed-at: "2025-07-04T17:45:00Z"