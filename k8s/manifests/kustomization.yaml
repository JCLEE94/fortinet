apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fortinet

resources:
  - namespace.yaml
  - persistent-volumes.yaml
  - pvc.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - test-deployment.yaml

images:
  - name: registry.jclee.me/fortinet
    newTag: f6b8bac

labels:
  - includeSelectors: true
    pairs:
      app: fortinet
      managed-by: argocd

commonAnnotations:
  app.kubernetes.io/version: "f6b8bac"
  deployed-by: "github-actions-debug"
  deployed-at: "2025-07-04T17:30:00Z"