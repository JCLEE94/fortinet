apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fortinet

resources:
  - namespace.yaml
  - persistent-volumes.yaml
  - pvc.yaml
  - redis.yaml
  - configmap.yaml
  - fortigate-dashboard-configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: python
    newTag: 3.11-slim

labels:
  - includeSelectors: true
    pairs:
      app: fortinet
      managed-by: argocd

commonAnnotations:
  app.kubernetes.io/version: "b0ddd71"
  deployed-by: "argocd-fix"
  deployed-at: "2025-07-07T07:29:00Z"